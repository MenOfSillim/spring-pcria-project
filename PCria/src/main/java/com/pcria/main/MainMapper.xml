<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Config 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.pcria.main.MainMapper">
	<select id="selFoodList" parameterType="FoodVO" resultType="FoodVO">
		SELECT seq, i_f, f_price, f_pic, f_name
		FROM t_food
		<if test="chk != 8">
		WHERE i_f = #{chk}
		</if>
	</select>
	
	<update id="updFood" parameterType="CountingDMI">
		UPDATE t_user 
		SET u_wallet = u_wallet - #{totalPayment}
		WHERE u_no = #{u_no}
	</update>
</mapper>